[pytest]
testpaths = tests
addopts = -q --strict-markers -m "not network"
asyncio_mode = auto
markers =
    network: needs the public internet (SatNOGS, Celestrak). Run with: pytest -m network
filterwarnings =
    ignore::DeprecationWarning
